{
  "name": "slutprojektet-start-template",
  "version": "2.0.1",
  "description": "Project to quickly get something working in p5.js and typescript",
  "scripts": {
    "start": "run-p compile-w serve",
    "compile-w": "tsc --watch",
    "compile": "tsc",
    "serve": "live-server --watch=index.html,build --port=5500 --wait=100"
  },
  "devDependencies": {
    "@types/jquery": "^3.5.13",
    "@types/p5": "^0.9.1",
    "live-server": "^1.1.0",
    "npm-run-all": "^4.1.5",
    "typescript": "^4.1.2"
  }
}
